---
interface Props {
  name: string;
  class?: string;
  size?: number;
}

const { name, class: className, size = 24 } = Astro.props;
---

<svg
  width={size}
  height={size}
  class={className}
  aria-hidden="true"
>
  <use href={`/icons/sprite.svg#icon-${name}`} />
</svg>
